<p id="notice"><%= notice %></p>

<p>
  <strong>Date:</strong>
  <%= @event.date %>
</p>

<p>
  <strong>Time:</strong>
  <%= @event.time %>
</p>

<p>
  <strong>Organiser:</strong>
  <%= @organiser.name %>
</p>

<p>
  <strong>Meetup Location:</strong>
  <a href= <%= "#{@event.location}" %> > <%= @event.location %> </a>
</p>

<p>
  <strong>Signed Up:</strong>
  <%= @signups.count + 1 %>
</p>



<strong> Already signed up: <br> </strong>
	<%= @organiser.name %> <br>

		<% @names.each do |loop| %>
		<% u = User.find(loop) %>
		<%= u.name %>
		<br>
		<% end %>

		<br><br>


<% if @names.include? current_user.id %>
			<%= link_to "Undo Your Signup", event_signup_url(@event) , class: "btn btn-sm btn-success", method: :delete, data: { confirm: 'Awww no, really??' } %> 
			<% end %>
			<br>

<br>
<br>




<!-- <%= link_to 'Back', events_path %> -->

<% if @organiser_id == current_user.id || @names.include? current_user.id %>
<% else %>
<%= link_to 'Join This Run', new_event_signup_url(@event), class: "btn btn-lg btn-primary" %>
<% end %>


<%= render inline: @map_embed %>

<% if current_user.id == @event.user_id %>
	<%= link_to 'Edit', edit_event_path(@event) %> 
<% else %>

<% end %>
<br>



